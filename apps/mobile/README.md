# apps/mobile

Expo + React Native + TypeScript app scaffold for Tenue.

Phase 03 adds the first real mobile slice:

- register and login screens
- secure session persistence with `expo-secure-store`
- API-backed auth provider state
- a protected shell route
- logout and session hydration behavior

Still out of scope:

- closet or any product-domain screens
- direct Supabase Auth integration
