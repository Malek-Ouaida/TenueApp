# Packages

This directory is for reusable JS/TS config and generated contract packages.

Start now:

- `config-typescript`
- `config-eslint`

Wait until real duplication exists:

- `api-types`
- `api-client`
- `design-tokens`
- `ui`

Do not add:

- `shared` catch-all packages
- shared backend business-logic packages
